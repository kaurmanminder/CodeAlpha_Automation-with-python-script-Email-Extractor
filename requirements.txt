# Requirements for Email Address Extractor and Saver
#
# Core functionalities use Python Standard Libraries:
# - re (regex operations)
# - os (system paths and folders)
# - sys (exit codes and streams)
# - argparse (CLI arguments)
#
# Optional Dependencies:
# Pillow is required for the automatic terminal screenshot feature.
# Run 'pip install -r requirements.txt' to install it.
Pillow>=10.0.0
